# React Component :

A react component in react is a re-usable piece of UI.

## Functional Component 
  
  ```js 
     function Card(){
         
         return (
            
         )
     }
  ```
  ## JSX 
  Javascript + XML(HTML like structure)

  ```js 

     
     function Card(){
         
         return (
              <div>
                  {
                    if(1){
                        alert("Working...");
                    }
                  }
                  <h1> This is JSX </h1>
                  <button> Click </button>
              </div>
         )
     }


  ```